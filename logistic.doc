MCTS:
	MCTS:
		输入上一次走的步，走完上一步之后棋盘的状况，以及是否还要继续游戏，继续游戏有三个状态，分别 是True（游戏继续），False（胜负已分），None（游戏已经结束但是平局）。
		
		None：如果是None,从当前的节点反向传播一个0.（这也就说明了，我们拓展了以后不能再拓展了。）
		无论True或False，如果已经拓展过一个树杈的话，直接完全随机选取走子了。因为如果已经拓展，就意味着后面的每一步我们都是未知的，也没有与之对应的分支。
			无论True还是False，都可以直接把游戏状态传导给树。树返回这个step对应的child，如果已经expand过了，也就意味着不会进入game_step这个函数了。

	game_step: 总的定义是，进入game step之后，必须要返回一个有意义的child，如果发现不能返回有异议的child，那么就代表着必须是expand了。然后本局游戏中都不会再进入这个函数中。
  

